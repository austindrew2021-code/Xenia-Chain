import './globals.css';
import type { Metadata } from 'next';
import { Inter } from 'next/font/google';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'BorroeFi - AI-Powered DeFi Lending on Solana',
  description: 'Revolutionary DeFi lending protocol with AI-optimized rates, ultra-low fees, and lightning-fast settlements on Solana blockchain.',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
